import { PageLayout } from 'renderer/layout/PageLayout';
import Datatable from '../components/DataTable';

const List = () => {
  return (
    <PageLayout>
      <Datatable />
    </PageLayout>
  );
};

export default List;

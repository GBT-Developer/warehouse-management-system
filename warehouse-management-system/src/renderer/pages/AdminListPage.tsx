import { PageLayout } from 'renderer/layout/PageLayout';
import Datatable from '../components/Table';

const List = () => {
  return (
    <PageLayout>
      <Datatable />
    </PageLayout>
  );
};

export default List;
